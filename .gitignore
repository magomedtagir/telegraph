# Секреты — НЕ коммитить (токены бота и Яндекс.Диска)
.env

# Зависимости
node_modules/

# Рантайм-кэш каталога (не исходник; hidden_imei.txt при этом трекается)
data/catalog-cache.json

# Мусор редакторов/ФС
*.fuse_hidden*
*.log
